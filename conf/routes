# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# API
POST     /time                 com.mtomanski.timer.infrastructure.play.controller.TimerController.addTime

# Views
GET      /                     com.mtomanski.timer.infrastructure.play.controller.ViewsController.renderTimer

GET      /avg/best             com.mtomanski.timer.infrastructure.play.controller.ViewsController.bestAverages

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

    